export interface ImageProps {
  label: string;
  src: string;
}

export interface ButtonProps {
  label: string;
  url: string;
}

export interface FeatureProps {
  title: string;
  description: string;
}
